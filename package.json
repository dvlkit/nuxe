{
  "name": "dvlkit-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "version": "pnpm changeset version",
    "release": "pnpm changeset publish",
    "build:all": "pnpm -r build"
  },
  "devDependencies": {
    "@changesets/cli": "2.27.0"
  }
}
